def cap_text(text):
    '''
    Input a string
    Output the capitalized string
    .capitalize: capitalize just the first letter of a string
    .title: capitalize every first letter for each string
    '''
    return text.title()